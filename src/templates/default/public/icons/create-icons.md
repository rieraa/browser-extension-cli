# 图标文件说明

此目录包含自动生成的图标文件：

- `icon16.png` - 16x16 像素
- `icon48.png` - 48x48 像素  
- `icon128.png` - 128x128 像素

这些图标是在初始化项目时从默认图标自动生成的。

## 自定义图标

如果你想使用自己的图标，可以：

1. 替换此目录中的图标文件
2. 或者修改 CLI 工具的默认图标源文件

图标要求：
- PNG 格式
- 透明背景（推荐）
- 正方形尺寸（会自动缩放）

